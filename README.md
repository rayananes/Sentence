This algorithm reads a sentence entered by the user and calculates:

The total number of characters (excluding the final period '.').

The total number of words in the sentence.

The total number of vowels (a, e, i, o, u) in the sentence.

The sentence is read character by character and stored in an array for processing
